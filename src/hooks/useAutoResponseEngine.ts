
import { useReliableAutoResponse } from './useReliableAutoResponse';

// Exportar la versión simplificada como la principal
export function useAutoResponseEngine() {
  return useReliableAutoResponse();
}
